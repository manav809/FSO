
const person = {
  name: "Manav",
  age: 21,
  isMarried: false,
};

const { name, age, isMarried } = person;

const person2 = {...person, name="MP"}

const names = ["k", "j", "m"]
const names2 = [...names, "l"]
